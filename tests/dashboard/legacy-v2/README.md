# Legacy Dashboard v2 scenarios

These fixtures and specs preserve the pre-Widget-v1 Dashboard contract for historical review. They cover the retired `/api/v2/dashboards/home` path, schema 2 drafts, virtual defaults and Home URL overrides. They are excluded from `playwright.dashboard.config.ts`; the active Dashboard suite is `../v3.spec.ts`.
